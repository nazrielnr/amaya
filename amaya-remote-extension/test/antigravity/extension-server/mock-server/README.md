# Mock Extension Server

Use this folder for local mock servers that simulate extension server events.

## Typical Files
- `test-raw-server.js`

## Output
- Mock stream outputs can stay in this folder when they are used as test fixtures.
